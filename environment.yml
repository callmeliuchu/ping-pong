name: pingpong-rl
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - gymnasium
      - stable-baselines3
      - pygame
      - pillow
      - numpy
      - tensorboard
